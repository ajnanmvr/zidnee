$kind: http-request
url: "{{baseUrl}}/api/users/{{userId}}/roles"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "roleId": "{{roleId}}"
    }
auth:
  type: bearer
  credentials:
    token: "{{authToken}}"
order: 3000
